# =============================================================================
# Despliegue en Railway — dos servicios + PostgreSQL
# =============================================================================
#
# Ver plantillas por servicio:
#   backend/railway.env.example   → servicio API (Root Directory: backend)
#   frontend/railway.env.example  → servicio SPA (Root Directory: frontend)
#
# Orden: PostgreSQL → backend (copiar URL) → frontend (VITE_API_URL) → CORS en backend
